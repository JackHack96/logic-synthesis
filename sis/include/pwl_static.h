
/* file @(#)pwl_static.h	1.2 */
/* last modified on 5/1/91 at 15:51:51 */
static double compute_intersect();

static double compute_max_slope();

static int compute_argmax_y();

static int next_point();

static int pwl_point_cmp();

static pwl_point_t *pwl_binary_search();

static pwl_t *compute_pwl_shift();

static pwl_t *create_linear_max();

static pwl_t *pwl_compute_min();

static pwl_t *pwl_compute_sum();

static void change_sign();

static void first_point();

static void pwl_point_print();
